// Re-exported from the shared location so every provider (DonTorrent,
// wolfmax4k, ...) uses the same criteria types.
export {
  SearchCriteria,
  SearchCriteriaSeason,
  SearchCriteriaEpisode,
} from "../SearchCriteria.ts";
